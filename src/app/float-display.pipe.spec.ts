import { FloatDisplayPipe } from './float-display.pipe';

describe('FloatDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FloatDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
